let name: string = 'carlos';
let hpPoints : number | string = 95; //let hpPoints : number | 'Palabra random' = 95; (esto quiere decir que puede ser un numero o la palabra entre comillas)
const isAlive: boolean = true;

hpPoints = 'eduardo';

console.log({name, hpPoints, isAlive});

export {};